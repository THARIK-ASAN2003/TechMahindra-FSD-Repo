import { Button } from "@chakra-ui/react";

const ButtonCmp = () => {
  return (
    <Button colorScheme="blue" size="md" _hover={{ bg: "blue.700" }}>
      Done
    </Button>
  );
};

export default ButtonCmp;
